**2026/01/23**
- ローカルにGOが入っていないのに、GOのコマンドを打ってエラー
	- winget install GoLang.Go でインストール(win環境)

- ローカルのGOのバージョンとDockerfileに記載したGOのバージョンが異なる
	- バージョンを合わせて解決

**2026/01/24**
- DBの関数内でdefer closeはしない
	- mainに書く

- インターフェースをポインタ型にしない
	- Goにおいて、インターフェース自体は「参照」のような性質を持っているため、ポインタを付けてはいけない

- メソッドにレシーバーを書き忘れた(r * authRepository)

- sqlboilerにてmodel.Usersこれが何か分かってなかった
	- boilerコマンドで生成したデータを指しているので、-p modelで生成したフォルダを指す