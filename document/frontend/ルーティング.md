Reactでページを切り替えるには**react-router-dom**を使う

App.tsx の構成例:
```
function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<PostList />} />
        <Route path="/signup" element={<Signup />} />
      </Routes>
    </BrowserRouter>
  );
}
```